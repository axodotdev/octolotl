{
  "name": "releases",
  "version": "0.0.0",
  "devDependencies": {
    "wrangler": "^3.3.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy-prod": "wrangler deploy --env prod",
    "deploy-staging": "wrangler deploy --env staging"
  }
}
